package br.com.argmax.githubconsumer.components.repositorycard.context

import org.junit.Before
import org.junit.Test

class RepositoryCardUserImageTest : RepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if user image is visible`() {
        `assert that user image is visible`()
    }

}