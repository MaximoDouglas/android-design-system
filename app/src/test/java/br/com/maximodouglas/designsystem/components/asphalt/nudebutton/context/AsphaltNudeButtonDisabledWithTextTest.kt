package br.com.maximodouglas.designsystem.components.asphalt.nudebutton.context

import org.junit.Before
import org.junit.Test

class AsphaltNudeButtonDisabledWithTextTest : AsphaltNudeButtonTest() {

    @Before
    fun `setup scenario`() {
        `when nude button is disabled`()
        `when nude button has text`()
    }

    @Test
    fun `test if nude button is disabled`() {
        `assert that nude button is disabled`()
    }

    @Test
    fun `test if nude button text is set correctly`() {
        `assert that text is set correctly`()
    }

}
