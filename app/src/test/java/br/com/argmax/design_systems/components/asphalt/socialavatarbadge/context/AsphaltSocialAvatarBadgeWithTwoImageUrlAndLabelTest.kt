package br.com.argmax.design_systems.components.asphalt.socialavatarbadge.context

import org.junit.Before
import org.junit.Test

class AsphaltSocialAvatarBadgeWithTwoImageUrlAndLabelTest : AsphaltSocialAvatarBadgeTest() {

    @Before
    fun `setup scenario`() {
        `when social avatar badge has two image urls and label`()
    }

    @Test
    fun `test if label text is set correctly`() {
        `assert that text is set correctly`()
    }

    @Test
    fun `test if image url list is set correctly`() {
        `assert that image url list is set correctly`()
    }

    @Test
    fun `test if main image is visible`() {
        `assert that main image is visible`()
    }

    @Test
    fun `test if secondary image is visible`() {
        `assert that secondary image is visible`()
    }

}
