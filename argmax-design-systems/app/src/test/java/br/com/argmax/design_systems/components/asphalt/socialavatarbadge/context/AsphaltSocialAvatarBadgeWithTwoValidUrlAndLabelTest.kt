package br.com.argmax.design_systems.components.asphalt.socialavatarbadge.context

import org.junit.Before
import org.junit.Test

class AsphaltSocialAvatarBadgeWithTwoValidUrlAndLabelTest : AsphaltSocialAvatarBadgeTest() {

    @Before
    fun `setup scenario`() {
        `when social avatar badge has two valid urls and label`()
    }

    @Test
    fun `test if label text is set correctly`() {
        `assert that text is set correctly`()
    }

    @Test
    fun `test if the image urls are set correctly`() {
        `assert that both image url are set correctly`()
    }

}
